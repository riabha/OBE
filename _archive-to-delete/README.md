# Archive — safe to delete later

This folder contains old documentation, one-off fix scripts, and legacy server files moved during project cleanup (June 2026).

**Nothing here is required to run the application.** The live stack uses:

- `server.js` + Docker (`docker-compose.yml`)
- Root `DEPLOYMENT.md` for all deployment instructions

When you are confident everything works, delete this entire `_archive-to-delete` folder.
